# Install on the production host: cp logrotate.conf /etc/logrotate.d/squirrel-api
# Adjust the path below to the actual deployment directory.
/opt/squirrel/log/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
