services:
  api:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
      - ./log:/app/log
    environment:
      - SQUIRREL_ENVIRONMENT=development
      - SQUIRREL_DATABASE_PATH=/app/data/squirrel.db
      - SQUIRREL_LOG_DIR=/app/log
    restart: always

  atlas:
    image: arigaio/atlas
    volumes:
      - .:/app
      - ./data:/data
    working_dir: /app

# No volumes section needed for bind mounts
